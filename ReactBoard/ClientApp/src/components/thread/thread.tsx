import React from 'react'

interface IThreadProps {
    id: number
}

const Thread = (props: IThreadProps) => {
    const { id } = props

    return (
        <>

        </>)
}

export default Thread